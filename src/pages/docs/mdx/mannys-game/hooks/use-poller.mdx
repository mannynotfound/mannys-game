import Code from '../../../Code'

# usePoller

Utility hook for calling a function on a set interval.

<h2 style={{ marginTop: 0 }}>Usage</h2>

<Code language="js">
{`const poll = () => fetchPrice();
const pollTime = 10000;
usePoller(pollPrice, pollTime);`}
</Code>




