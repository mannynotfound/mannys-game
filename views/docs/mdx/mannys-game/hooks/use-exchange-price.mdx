import Code from '../../../Code'

# useExchangePrice

Hook for getting current exchange price of Ethereum.

<h2 style={{ marginTop: 0 }}>Usage</h2>

<Code language="js">
{`const pollTime = 10000;
const balance = useBalance({ addressOrName: 'mannydao.eth' });
const usd = (balance * exchangePrice).toLocaleString(undefined, {
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
});`}
</Code>

## Return Value

<Code language="js">
{`float // current price of Ethereum in USD`}
</Code>
